{
  "name": "twinvest-app",
  "version": "1.0.0",
  "description": "TwinVest - KB MyData Behavioral Economics Financial Digital Twin App",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Building web assets...'",
    "cap:add": "npx cap add android",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
